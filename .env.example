# Supabase — copy this file to .env and fill in real values (local dev).
# Find them in: Supabase Dashboard → Project Settings → API
# Production values are set in Vercel project settings, not here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
